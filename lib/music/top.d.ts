/**
 * @param {number} limit - Limit of songs to fetch from Billboard's top 100 (set to 10 by default)
 * @returns {JSON} - Array of songs
 */
export declare function endpoint(limit?: number): Promise<any>;
